AI search needs JSON files for KBs (Knowledge bases)

In the example readme, convert it to a JSON and store it in the JSON folder. when AI Search is configured, it is configured for JSON input. 

Text was used before, but this is a slow process with the current search ranking setup.